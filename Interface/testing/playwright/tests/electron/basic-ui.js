
/* test.describe('Main elements exist', () => {
  test('Basic elements present', async ({ window }) => {
    // Check if working-area exists
    const workingArea = window.locator('#working-area');
    await expect(workingArea).toBeVisible();

    const sidebar = window.locator('#sidebar');
    await expect(sidebar).toBeVisible();

    const controlPanel = window.locator('#working-area #main-menu');
    await expect(controlPanel).toBeVisible();
  });
});
 */